#include <stdio.h>

/*
 * Exercise 1-1. Run the "hello world" program on yoyr system. Experiment with
 * leaving out parts of the program, to see what error messages you get.
 *
 * Simply use the printf function.
 *
 * The printf takes a format string and variables which must match the format
 * arguments which are something like %<something>.
 */
int main(void) {
  printf("Hello, world\n");
  return 0;
}
